[project]
name = "digikey-mcp"
version = "0.1.0"
description = "DigiKey MCP Server for product search"
dependencies = [
    "fastmcp>=2.10.0",
    "requests",
    "python-dotenv",
    "jmespath",
]
requires-python = ">=3.10" 
